# direnv configuration for Zoned project
# This file enables convenient VM management commands without path prefixes
#
# Setup:
#   1. Install direnv: https://direnv.net/
#   2. Run: direnv allow
#   3. Now use: vm setup, vm logs, vm profile list, etc.
#
# Without direnv, use: ./scripts/vm <command>

# Add scripts directory to PATH so 'vm' command is available
export PATH="$PWD/scripts:$PATH"
